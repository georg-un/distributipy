language: python
python:
  - "3.5"
  - "3.6"
install:
  - pip install -r requirements.txt
  - pip install codecov
script:
  - travis_wait 20 python -m unittest discover
  - travis_wait 20 coverage run -m unittest discover
after_success:
  - codecov
